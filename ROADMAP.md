แผนนี้เริ่มจากของที่มีอยู่: gateway พร้อม, Text-to-SQL พร้อม, ชั้นแอปเป็นคนจัดโฟลว์  
12 สัปดาห์แรกทำให้เป็น AI Engineer ที่ใช้ชื่อได้  
หลังนั้นเป็นช่วงขยายไป Full

สมมุติมีเวลาประมาณ **8–12 ชั่วโมงต่อสัปดาห์**  
ถ้ามีมากกว่านี้ย่อสัปดาห์ได้ แต่ลำดับห้ามสลับ

---

## สัปดาห์ที่ 1 — คลังเอกสารเล็ก
เป้า: มีเอกสารจริงเข้าคลัง ไม่ใช่ทฤษฎี

- เลือกเอกสาร 1 ชุดที่ตารางตอบไม่ได้ เช่น คู่มือหรือนโยบาย 20–50 หน้า
- แปลงเป็นข้อความสะอาด
- ตัดเป็นชิ้น 300–800 โทเคน เหลื่อมกันเล็กน้อย
- เก็บ `doc_id`, `chunk_id`, `text`, `source` ใน Postgres

จบเมื่อ: สุ่มเปิด 10 ชิ้นแล้วยังอ่านรู้เรื่อง ไม่ขาดกลางประโยคทั้งก้อน

## สัปดาห์ที่ 2 — ฝังและค้นได้
เป้า: คำถามสั้นหาชิ้นที่ถูกได้

- ทำ embedding เก็บคอลัมน์เวกเตอร์ (pgvector พอ)
- ค้น top-5 ด้วยความคล้าย
- เขียน API ชั้นแอป `search_docs(question) → chunks`
- คำตอบยังไม่ต้องสวย แค่ชิ้นที่ดึงมาต้องเกี่ยว

จบเมื่อ: คำถาม 10 ข้อ อย่างน้อย 7 ข้อชิ้นแรก ๆ ตรงเรื่อง

## สัปดาห์ที่ 3 — ตอบจากชิ้น ไม่มโน
เป้า: มีเส้น `doc` จริง

- ส่งเฉพาะชิ้นที่ค้นได้เข้า gateway
- บังคับชี้แหล่ง
- ถ้าคะแนนต่ำหรือไม่มีชิ้น → ตอบว่าไม่มีในเอกสาร
- ห้ามใช้ความรู้โมเดลเติมกติกาบริษัท

จบเมื่อ: ถามของที่ไม่มีในคลังแล้วระบบยอมไม่รู้

## สัปดาห์ที่ 4 — hybrid + ตัดชิ้นขยะ
เป้า: ค้นแม่นขึ้น

- ค้นคำตรง ๆ ร่วมกับความหมาย แล้วรวม
- ทิ้งชิ้นคะแนนต่ำ
- ทดลองขนาดชิ้น 2–3 แบบ แล้วเทียบ 10 คำถามเดิม
- เก็บตารางผลไว้

จบเมื่อ: รู้ว่าแบบไหนแม่นกว่า ไม่ใช่เดา

## สัปดาห์ที่ 5 — Router สี่ทาง
เป้า: ไม่ปน SQL กับเอกสารพร่ำเพรื่อ

ทาง:
- `general` ตอบตรง
- `db` โฟลว์ตารางเดิม
- `doc` เส้นใหม่
- `both` เฉพาะคำถามที่ต้องการสองแหล่ง

จบเมื่อ: คำถามตัวเลขไม่ไปค้นคู่มือ คำถามนโยบายไม่ไปเขียน SQL

## สัปดาห์ที่ 6 — รวมหลักฐาน
เป้า: โหมด `both` ใช้ได้โดยไม่กลบความจริง

- ผล SQL ติดป้าย `from_db`
- ชิ้นเอกสารติดป้าย `from_doc`
- ชนกันต้องบอกว่าชน
- ไม่ยิงทั้งคู่ทุกคำถาม

จบเมื่อ: มี 10 คำถามผสม ตอบครบและชี้ได้ว่าตัวเลขมาจากไหน กติกามาจากไหน

## สัปดาห์ที่ 7 — ชุดทองและ regression
เป้า: เปลี่ยน prompt แล้วรู้ว่าพัง

ทำชุดละอย่างน้อย:
- 10 ข้อตาราง
- 10 ข้อเอกสาร
- 10 ข้อผสม
- 5 ข้อที่ต้องไม่รู้

เก็บ: แหล่งถูกไหม, มโนไหม, SQL นอก allowlist ไหม, เวลา, โมเดลที่ใช้

จบเมื่อ: รันชุดเดิมซ้ำได้หลังแก้ระบบ

## สัปดาห์ที่ 8 — Agent เลือกเครื่องมือ
เป้า: โมเดลเลือกทางเองได้ในกรอบ

เครื่องมือชั้นแอป:
- ค้นตาราง/SQL
- ค้นเอกสาร
- ค้นเว็บถ้ามี
- ตอบจบ

จำกัดรอบ ไม่ให้วน  
เครื่องมือพังแล้วหยุดหรือสลับโมเดลที่แอป

จบเมื่อ: งานง่ายไม่เปิดเครื่องมือเกินจำเป็น

## สัปดาห์ที่ 9 — คุณภาพและต้นทุนที่แอป
เป้า: สลับโมเดลตามงาน ไม่ใช่ที่ gateway

- งานแยกทางใช้โมเดลถูก
- เขียน SQL / รวมหลักฐานใช้โมเดลกลาง–เก่ง
- นับราคาต่อคำถามจาก log
- ตัดคำถามที่แพงแต่ไม่ได้แม่นขึ้น

จบเมื่อ: อธิบายได้ว่างานไหนทำไมไม่เริ่มที่โมเดลถูกสุด

## สัปดาห์ที่ 10 — เคสที่เล่าได้
เป้า: สัมภาษณ์ได้โดยไม่กินคำ

เขียนหน้าเดียว:
- ปัญหา
- ทำไมตารางอย่างเดียวไม่พอ
- ไหลงาน
- จุดพัง (10 แถวล่าสุดหลอก, เอกสารเก่าชนตาราง)
- ตัวเลขชุดทอง

จบเมื่อ: พูดครบใน 5 นาที

## สัปดาห์ที่ 11–12 — ขัดของจริง
เป้า: ของใช้ได้ ไม่ใช่เดโม

- เอกสารเพิ่มเป็นชุดที่ใช้จริง
- แก้เคสที่ชุดทองยังพลาด
- ล็อกไม่มโนให้แน่น
- อย่าเริ่ม fine-tune ในสองสัปดาห์นี้

จบระยะ 12 สัปดาห์เมื่อมีระบบตอบจากตารางและเอกสาร วัดได้ อธิบายจุดพังได้  
แค่นั้นใช้ชื่อ AI Engineer ได้

---

## เดือนที่ 4–5 — ชั้นโมเดล (Full เริ่มตรงนี้)

เรียน Python สำหรับงานโมเดลควบกับของเดิม

สัปดาห์ละหนึ่งเรื่อง อย่าขนคอร์สทั้งดุ้น:
- เวกเตอร์และความคล้าย
- เลือก embedding แล้วเทียบบนคลังเดิม
- rerank
- train/val/test คืออะไร
- fine-tune เล็ก 1 งาน เช่น จัดประเภท `general/db/doc/both` แล้วเทียบกับให้ LLM จัดเอง

จบเมื่อ: มีตารางเทียบ API อย่างเดียว vs ของที่ปรับเอง

## เดือนที่ 6–7 — ทดลองเป็นนิสัย

- อ่านเปเปอร์/บทความวิธี RAG หรือ agent แล้วลงของจริง
- วัดก่อน–หลัง ทุกครั้ง
- ของที่ไม่ได้ผลทิ้ง
- เริ่มเวอร์ชันคลังเอกสารและชุดทอง

## เดือนที่ 8–9 — สายภาพถ้ายังจะเอาหมด

ต่อจากกล้องทักสวัสดี:
- รู้ว่าใคร ไม่ใช่แค่มีคน
- วัดหลุด/ทักผิด
- แยกบริการ ไม่ปนกับแชทเอกสาร
- ต่อหุ่นยนต์เมื่อเส้นนี้เสถียร

---

## กฎแผนนี้

- สัปดาห์ 1–3 ห้ามผสม SQL
- สัปดาห์ 1–12 ห้าม fine-tune
- gateway ห้ามโตเป็นตัวจัดโฟลว์
- แต่ละสัปดาห์มีของส่ง ไม่มีสัปดาห์ที่มีแต่ดูคลิป

สัปดาห์ 1 เริ่มวันนี้ได้ด้วยงานเดียว: เลือกเอกสารชุดแรกแล้วตัดเป็นชิ้นเก็บลงตาราง  
ถ้าต้องการขั้นถัดไปเป็น schema ตาราง `documents` / `chunks` บอกประเภทไฟล์ที่จะใช้ได้เลย