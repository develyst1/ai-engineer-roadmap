# สัปดาห์ที่ 1 — คลังเอกสารเล็ก

เป้า: มีเอกสารจริงอยู่ในตาราง ไม่ใช่ทฤษฎี  
สัปดาห์นี้ **ยังไม่มี embedding ยังไม่มี LLM** แตะแค่ข้อความกับตาราง

---

## เลือกเอกสารก่อน

เกณฑ์: ต้องเป็นของที่ **ตารางตอบไม่ได้**

ตัวอย่างที่ใช้ได้ — คู่มือพนักงาน, ระเบียบลาและสวัสดิการ, SOP,
เอกสารสัญญา/ข้อกำหนดบริการ, คู่มือระบบภายใน

ตัวอย่างที่ไม่ควรใช้รอบแรก — สลิปเงินเดือน, รายงานตัวเลข, export จาก DB
(พวกนี้เป็นงานของเส้น `db` ที่มีอยู่แล้ว เอามาปนจะสับสน)

ขนาด 20–50 หน้า พอ อย่าเยอะกว่านี้ในรอบแรก

---

## Schema

```sql
CREATE EXTENSION IF NOT EXISTS vector;   -- ยังไม่ใช้สัปดาห์นี้ แต่เปิดไว้เลย

CREATE TABLE documents (
  doc_id        TEXT PRIMARY KEY,          -- เช่น 'hr-handbook-2026'
  title         TEXT NOT NULL,
  source        TEXT NOT NULL,             -- path หรือ URL ต้นทาง
  doc_type      TEXT NOT NULL,             -- 'policy' | 'sop' | 'manual' | 'contract'
  lang          TEXT NOT NULL DEFAULT 'th',
  version       TEXT,                      -- ฉบับ/ปี ที่ระบุในเอกสาร
  effective_at  DATE,                      -- วันที่มีผลบังคับ (สำคัญตอนสัปดาห์ 6)
  content_hash  TEXT NOT NULL,             -- sha256 ของข้อความสะอาด กัน ingest ซ้ำ
  ingested_at   TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE TABLE chunks (
  chunk_id      TEXT PRIMARY KEY,          -- '{doc_id}#{seq}'
  doc_id        TEXT NOT NULL REFERENCES documents(doc_id) ON DELETE CASCADE,
  seq           INT  NOT NULL,             -- ลำดับในเอกสาร เริ่ม 0
  text          TEXT NOT NULL,
  token_count   INT  NOT NULL,
  page_from     INT,                       -- ไว้อ้างอิงตอนตอบ
  page_to       INT,
  heading_path  TEXT,                      -- 'หมวด 3 > การลา > ลากิจ'
  embedding     vector(1024),              -- NULL สัปดาห์นี้ เติมสัปดาห์ 2
  UNIQUE (doc_id, seq)
);

CREATE INDEX ON chunks (doc_id);
```

**เหตุผลที่ใส่ฟิลด์พวกนี้ตั้งแต่แรก** — ตอนสัปดาห์ 3 ต้องชี้แหล่งได้
(`page_from` / `heading_path`) ตอนสัปดาห์ 6 ต้องบอกได้ว่าเอกสารเก่าชนตาราง
(`effective_at` / `version`) เพิ่มทีหลังแปลว่า re-ingest ทั้งคลัง

`vector(1024)` ปรับตามโมเดล embedding ที่จะเลือกสัปดาห์หน้า
(ไทยควรใช้ multilingual — ตัดสินใจสัปดาห์ 2 ไม่ใช่สัปดาห์นี้)

---

## ลำดับงาน

1. **แปลงเป็นข้อความสะอาด** — PDF ใช้ตัวที่รักษาลำดับได้
   ตรวจด้วยตาว่าหัวข้อ/ตารางไม่เละ ถ้าเป็น scan ต้อง OCR ก่อน
   และถ้า OCR ออกมาแย่ ให้เปลี่ยนเอกสาร อย่าฝืน
2. **ล้าง** — ตัด header/footer ที่ซ้ำทุกหน้า, เลขหน้าเดี่ยว ๆ,
   ยุบช่องว่างซ้อน, ต่อคำที่ถูกตัดท้ายบรรทัด
3. **ตัดชิ้น 300–800 โทเคน เหลื่อม ~15%**
   ตัดที่ **ขอบเขตหัวข้อก่อน** แล้วค่อยขอบเขตย่อหน้า
   ห้ามตัดกลางประโยค ห้ามตัดตามจำนวนตัวอักษรดิบ
   ภาษาไทยไม่มีเว้นวรรคระหว่างคำ — นับโทเคนด้วย tokenizer จริง อย่าเดาจาก `len()`
4. **บันทึกลงตาราง** พร้อม `heading_path` และเลขหน้า
5. **เขียน ingest ให้รันซ้ำได้** — เช็ค `content_hash` ถ้าซ้ำให้ข้าม
   ถ้าเปลี่ยนให้ลบ chunks เดิมของ `doc_id` นั้นแล้วใส่ใหม่

---

## กับดักที่จะเจอ

- **ตัดตามจำนวนตัวอักษร** → ชิ้นภาษาไทยจะสั้นเกินจริง ความหมายขาด
- **เหลื่อมมากเกินไป** (>30%) → ค้นเจอชิ้นซ้ำเต็มไปหมดตอนสัปดาห์ 2
- **โยนทั้งหน้าเป็นชิ้นเดียว** → ค้นเจอ แต่ context เต็มไปด้วยของไม่เกี่ยว
- **ทิ้ง heading** → สัปดาห์ 3 จะชี้แหล่งไม่ได้ ต้องกลับมาทำใหม่
- **ingest ครั้งเดียวจบ ไม่ทำ idempotent** → แก้ chunker ทีต้องล้างมือ

---

## จบเมื่อ

- สุ่มเปิด 10 ชิ้น อ่านรู้เรื่องทั้ง 10 ไม่มีชิ้นไหนขาดกลางประโยค
- ทุกชิ้นย้อนกลับไปหาหน้า/หัวข้อต้นทางได้
- รัน ingest ซ้ำแล้วจำนวน chunk ไม่เพิ่ม
- `SELECT count(*) FROM chunks` ได้ประมาณ 40–150 สำหรับเอกสาร 20–50 หน้า
  (ถ้าได้ 10 คือชิ้นใหญ่ไป ถ้าได้ 800 คือชิ้นเล็กไป)

**ห้ามทำสัปดาห์นี้:** embedding, เรียก LLM, ต่อกับเส้น SQL เดิม
